Traffic Control Mod 1.21.11-2.0.0 (Beta 1) - NeoForge 21.11.42 - Session Changelog (4/14-16/26)
======================================================================================================

### Commits
1. Started porting Traffic Control mod from Forge 1.12.2 to NeoForge 1.21.11
2. Added changes to changelog-1.21.11
3. Added some functionality to the horizontal pole (WIP)

### Traffic Light Frame Variants
- Added yellow traffic light frame variants (7 shapes: triple, single, double, quad, five, T, doghouse)
- Added orange traffic light frame variants (7 shapes: triple, single, double, quad, five, T, doghouse)
- Total: 21 frame items across black, yellow, and orange color families
- Frame item IDs changed from old mod format to new naming convention:
  - Black: traffic_light_frame, traffic_light_1_frame, traffic_light_2_frame, etc.
  - Yellow: yellow_traffic_light_frame, yellow_traffic_light_1_frame, etc.
  - Orange: orange_traffic_light_frame, orange_traffic_light_1_frame, etc.
- Frames now use ItemTrafficLightFrame class (extends BlockItem) instead of simple BlockItem

### Ghost Block Preview
- Added GhostBlockRenderer.java — renders a translucent (40% opacity) preview of the traffic light at the placement position whenever the player holds a traffic light frame item
- Shows at any position the player is looking at, not just near poles
- Dynamically shows/hides the horizontal bar based on whether poles are adjacent to the placement position
- Works for both main hand and offhand
- Applies correct rotation matching player facing direction
- Limited to 10 blocks max render distance
- Handles both block hits and air/miss hits (for creative flying)

### Extended Placement Range
- Modified ItemTrafficLightFrame.java — added BLOCK_INTERACTION_RANGE attribute modifier (+3.5 blocks) so traffic lights can be placed from 8 blocks away (survival) / 8.5 blocks (creative) instead of the default 4.5/5

### Horizontal Pole Hitbox
- Updated BlockHorizontalPole.java — selection shape (outline/hitbox) now matches the actual thin pole dimensions, with collision shape matching (reverted from full block)

### Horizontal Pole Model
- Extended horizontal_pole.json dimensions (Z=-9 to Z=25) so the bar properly connects to traffic light frames in all directions
- Signal arm bar in RotatableBlockEntityRenderer.java now uses horizontal_pole model for all directions

### Traffic Light Placement (WIP) (Beta 2)
- Traffic lights can be mounted on crossing gate poles, horizontal poles, and crossing gate bases
- Pole-mounted TLs shift 9/16 toward the pole for flush mounting
- Side-by-side TL rows connect flush (no pole shift when adjacent TLs detected)
- Back-to-back TL pairs across a pole block are detected and handled
- TLs become transparent (click-through) when holding a TL frame for easy row placement
- Hitbox uses split top/bottom shapes for solo pole-mounted TLs, normal shapes for side-by-side rows
- HP placement logic in ItemTrafficLightFrame finds empty positions around horizontal poles
- CG_POLE connects to BlockHorizontalPole, BlockTrafficLight, BlockCrossingGatePole via blockstate arms
- HP connects to BlockTrafficLight, BlockCrossingGatePole, BlockCrossingGateBase, BlockHorizontalPole
- HP renders ext arms toward adjacent TLs, CG poles, and CG bases
- Non-cardinal TL rotations use short pole arm model (traffic_light_pole_arm) to prevent bar poke-through
- TL-to-pole connecting bar skipped for non-cardinal rotations to avoid clipping through angled body
- Added new models: signal_arm_bar.json, traffic_light_paired.json, traffic_light_pole_arm.json
- Removed redundant full-length bar from TL toward horizontal pole (HP already renders short arm, TL shift bridges the gap)
- Added short pole arm from TL toward crossing gate pole for CG-pole-mounted TLs
- Fixed gap when TL is between HP and CG pole: TL no longer shifts when it has pole connections on both sides
- HP now renders ext arms toward back-to-back TL pairs for proper pole connection (same as crossing_gate_pole)
- Fixed bug: TL hitboxes now always visible/breakable even when holding a TL frame item
- Removed signal_arm block, item, model, and blockstate (replaced by direct connections between traffic lights, crossing gate poles/bases, and horizontal poles)

### Traffic Light Bulbs
- Added all 18 traffic light bulb items as separate registered items (NeoForge 1.21 no longer supports metadata-based item variants)
- Basic bulbs: Red, Yellow, Green
- Left arrow bulbs: Red, Yellow, Green Arrow Left
- Right arrow bulbs: Red, Yellow, Green Arrow Right
- Straight arrow bulbs: Straight Red, Straight Yellow, Straight Green
- Pedestrian bulbs: Cross, Don't Cross
- No turn bulbs: No Right Turn, No Left Turn
- Tunnel bulbs: Tunnel Green, Tunnel Red (restored from v0.4.1)
- Fixed texture paths from blocks/ to block/ for NeoForge 1.21 compatibility
- Added item definition files in assets/trafficcontrol/items/ (required by NeoForge 1.21)
- All bulbs appear in the Traffic Control creative tab with 3D models and textures

### New Creative Tab Items
- Added 19 placeholder blocks to the creative tab: Pedestrian Button, Crossing Gate, Crossing Lamps, Crossing Gate Crossbuck, Overhead (Pole), Overhead, Overhead Lamps, Overhead Crossbuck, Sign, Upper Quadrant Wig Wag, Lower Quadrant Wig Wag, SafeTran Type 3 Electronic Bell, SafeTran Mechanical Bell, WCH Mechanical Bell, WCH Electronic Bell, Street Sign, Traffic Sensor (Left), Traffic Sensor (Straight), Traffic Sensor (Right)
- Fixed all old Forge blockstates to NeoForge 1.21 format
- Fixed texture paths from blocks/ to block/ for all new block models
- Added sensor display transforms matching old mod's inventory rendering
- Street sign uses flat 2D item model with item/generated parent

### Sign Block
- BlockSign now uses ROTATION_16 for 16-direction rotation (was HorizontalDirectionalBlock with 4 directions)
- Sign faces toward the player when placed, supports all 16 angles like traffic lights
- RenderShape.INVISIBLE — block entity renderer handles body + arms with cutout render type
- Sign inventory icon uses flat 2D circle texture instead of 3D block model
- Separated block model (sign_block.json, pole only) from item model (sign_item.json, pole + static face)

### Sign GUI
- Ported sign selection GUI from 1.12.2 GuiScreen to NeoForge 1.21 Screen API
- Right-click sign block to open GUI with searchable grid of all 900+ sign textures
- Search box filters signs by name in real-time using EditBox with setResponder
- Sign grid renders 32×32 thumbnails with 4-column layout and scroll support
- Mouse wheel scrolling and draggable scrollbar for navigating the full sign list
- Blue hover highlight and green selection highlight (2px borders) on sign thumbnails
- Hovered sign name displayed at bottom of grid panel
- Left panel shows large preview of selected sign with name, type, and note text
- Default blank circle sign rendered when no sign is selected
- Sign selection saved to SignBlockEntity and synced to server via PacketUpdateSign on GUI close

### Sign Renderer (Dynamic)
- Sign face texture rendered dynamically by RotatableBlockEntityRenderer using submitCustomGeometry
- Front face rendered with entityCutout render type at z=0.431, facing -z (north in local space)
- Back face rendered 0.01 behind front face at z=0.441, facing +z (south in local space)
- Both faces rotate with the block's ROTATION_16 property
- Sign textures loaded on-demand via ensureTextureLoaded (registers SimpleTexture if not already loaded)
- signFrontTexture and signBackTexture fields added to RotatableBlockEntityRenderState
- Falls back to DEFAULT_BLANK_SIGN texture when no sign is selected on the block entity

### Sign Pole Connections & Mounting (WIP) (Beta 2)
- Signs render short arms (traffic_light_pole_arm model) toward adjacent CG poles, CG bases, and traffic lights
- Signs do NOT render arms toward horizontal poles — the HP renders its own short arm toward the sign
- HP renders short arm (traffic_light_pole_arm) toward adjacent signs to avoid double-arm overlap
- Signs shift 9/16 toward CG pole/base when mounted (not toward HP — stays centered)
- Sign face, back face, and arms all shift with body when mounted on CG pole
- Sign-to-sign chaining: signs detect adjacent signs and render short arms (horizontal_bar_connect, 2px)
- Chained signs detect CG pole 2 blocks away (through adjacent sign) and shift toward it
- Back-to-back signs: adjacent signs facing opposite directions (rotation diff of 8) snap together
- Both shift 9/16 toward each other, no arms rendered between them
- Signal arm bar bridge rendered between the two poles
- Rotation-aware hitbox: thin slab for cardinal rotations, full block for diagonal rotations
- Hitbox shifts with body when mounted on CG pole/base or chained
- No collision shape — players walk through signs

### Traffic Light & Sign Back-to-Back (Beta 3.2) (4/10-4/14/26)
- Adjacent traffic lights facing opposite directions (rotation diff of 8) connect back-to-back
- Back-to-back detection uses TL's own back direction (rotation-based), not partner position
- Tiebreaker system: only second TL/sign shifts (8/16), first stays at block center
- Bridge bar uses B2B_STUB_MODEL_KEY (b2b_stub.json, 4px) — tip stays behind sign face with 5/16 offset
- Created separate b2b_stub.json model (z=0-4) to prevent poke-through without affecting CG pole arms
- 5/16 offset tiebreaker — stubs meet between frames
- TL-to-CG pole arm removed — CG pole handles arm rendering (DO NOT #6)
- Direct back-to-back paired detection in blockstate (paired: true for adjacent opposite-facing TLs/signs)
- Across-pole paired check now scans ALL pole directions (not just first)
- Sign back-to-back arms skipped (skipSignArms) when backToBackSignDir is set — prevents unshifted arms poking through shifted sign face
- TL back-to-back hitbox: second TL shifts 8px toward partner using full TL frame shape + baseShape.move()
- Sign b2b hitbox tiebreaker fixed: now checks ALL directions for partner (matches renderer), not just back direction

### Sign Pole Connections & Mounting (Beta 3.2) (4/14/26)
- Signs now support back-to-back connections matching TL frame behavior
- BlockSign: added HAS_HORIZONTAL_BAR and PAIRED blockstate properties
- BlockSign: added updateConnections() with direct + across-pole pairing
- BlockSign hitbox: HP mount detection added, shift amounts per mount type (HP=7px, CG=9px, chained=8px, b2b=8px)
- sign.json blockstate: explicit variants for horizontal_bar/paired combos
- Renderer: sign back-to-back detection works even when mounted on a pole (removed !mountedOnPole guard)
- Renderer: chainedSignMount flag for second sign in chain
- Sign body model (center post) always renders — skipBodyModel only for street signs
- BlockCrossingGateBase added to sign renderer mount detection (was missing, only checked CG pole)

### Crossing Gate Pole/Base Arms (Beta 3.2) (4/12-4/14/26)
- CG pole/base arms now use HORIZONTAL_BAR_CONNECT_MODEL_KEY (7px connect model)
- Removed signDirs skip — CG pole arms render toward signs AND TLs (not skipped)
- CG base now renders connection arms toward adjacent TLs/signs (same rendering as CG pole)
- BlockCrossingGateBase added to isVerticalPoleBlock() for mount detection
- CG base detects adjacent BlockTrafficLight and BlockSign for arm rendering
- Created cg_pole_arm.json (z=0.5-7, 6.5px) and CG_POLE_ARM_MODEL_KEY — registered for future use
- Mount priority: HP first, CG pole/base second (same for signs and TLs)

### Hitbox Improvements (Beta 3.2) (4/14/26)
- TL frame hitboxes: 4-bulb (y=0-32) and 5-bulb (y=-5 to 32) taller shapes matching model height
- TL frame hitboxes: pole-mounted uses full frame shape shifted toward pole (9/16 CG, 7/16 HP)
- TL frame hitboxes: arm extensions toward all connected neighbors (HP, CG poles, TLs, signs)
- Horizontal TL frame hitboxes: per-variant width (3-bulb: 30px, 4-bulb: 28.5px, 5-bulb: 34.5px)
- HP hitboxes: arm extensions now include CG poles, CG bases, signs, street signs, and adjacent HPs (was TL-only)
- getBaseShape() helper selects correct shape per TL variant (3/4/5-bulb)

### HP-Mounted TL Shift (4/12/26)
- HP-mounted TLs shift 7/16 toward HP (was 9/16)
- shiftToPole guards bypass for backToBackTLDir only (HP bypass causes side-by-side gaps)
- HP arm toward TLs stays at TRAFFIC_LIGHT_POLE_ARM_MODEL_KEY (7px)

### Screwdriver Item
- Ported screwdriver from 1.12.2 to NeoForge 1.21.11
- Right-click on any Traffic Control block to rotate clockwise (ROTATION_16 +1 or FACING 90°)
- Shift+right-click to rotate counter-clockwise
- 128 durability, plays screwdriver sound on use, no damage in creative
- 3D item model with existing textures (screwdriver_top, screwdriver_metal, shunt)
- Tooltip: "Hold SHIFT for more info" expands to full description
- Sign GUI suppressed when holding screwdriver (block checks for screwdriver in hand)

### Horizontal Traffic Light Frames (WIP) (Beta 2) (4/10/26)
- Added 9 new horizontal/sideways traffic light frame variants:
  - 3-bulb, 4-bulb, and 5-bulb sizes
  - Black, yellow, and orange colors
- Models rotated 90° around Z axis from vertical versions with vertical back pole
- Registered as BlockTrafficLight with same placement behavior as vertical frames
- Added to creative tab after orange doghouse frame
- Fixed back pole cross piece: centered and sized correctly per frame width (2/4/5-bulb)
- Fixed hitbox: horizontal frames use wider VoxelShape matching model bounds
- Horizontal frames no longer shift toward pole when mounted on horizontal poles
- HP extends full-length bar into horizontal TL frame blocks for proper connection
- Horizontal frames excluded from shiftToPole, bar-through-frame, and pole-shifted hitbox logic

### 3D Inventory Icons (4/6/26)
- Upgraded all 21 vertical traffic light frame inventory icons from 2D sprites to 3D models
  - Item models are copies of block models with Back Pole removed
  - Per-size GUI display scaling: 1-bulb: 0.625, 2-bulb: 0.7, 3-bulb: 0.625, 4-bulb: 0.55, 5-bulb: 0.475, T-frame: 0.5, doghouse: 0.5
  - Horizontal frames have separate scale/translation values for proper centering
  - Old 2D PNG sprite textures kept in textures/item/ as fallback
- Traffic light bulb icons slightly angled (rotation [5, -10, 0]) for 3D look while keeping arrows readable

### Gray Bulb Background Texture (4/6/26)
- Added gray.png texture for bulb recesses in all traffic light models (block and item)
- Integrated as texture #3 in all TL block models
- Bulb elements use #3 on south face (visible) and #2 on other faces for block models
- Item models use #3 on all faces to prevent bleed-through at inventory angles

### Back Pole and Back Pole Arm (4/5/26)
- Added missing Back Pole and Back Pole Arm to all horizontal TL models
- Shortened Back Pole (Y 3-12) with horizontal cross piece
- Matches the 3-bulb horizontal model structure

### 2-Bulb Horizontal Variants (4/5/26)
- Added 2-bulb horizontal traffic light frame variants (black, yellow, orange)
- Block models, item models, blockstates, and item definitions

### Creative Tab Reorganization (4/6/26)
- Reordered creative tab by color: all black (vertical + horizontal), all yellow, all orange
- Within each color: single, double, triple, quad, five, T, doghouse, then horizontal variants

### Street Sign Block Rework (4/6/26)
- New BlockStreetSign class with ROTATION_16 for 16-direction rotation
- Places facing player direction, rotatable with screwdriver
- Rotation-aware hitbox (NS/EW/diagonal), no collision (walk through)
- Emits light level 15 (fullbright, shade:false on model)
- Renders vertical pole connection when CG pole is below (scaled to sign plate height)
- CG pole arms extend toward adjacent street signs
- Blank green texture (no text) for placed block, 2D sprite with text for inventory
- Registered in ModBlockEntities ROTATABLE

### Hanging Street Signs (4/7/26)
- Both street sign and illuminated street sign support hanging placement
- Place on bottom face of a block to hang (HANGING blockstate property)
- Sign plate shifts up to Y 15-19, close to the block/pole above
- Vanilla-style chain bracket renders above the sign (metal texture, ±45° crossed planes)
- Chain bracket model (hanging_bracket.json) spans Y 19-24, rotates with sign body
- Hanging hitbox extends above block boundary (Y 15-19) to match rendered position
- Blockstate files updated to 32 variants (16 rotations × hanging true/false)
- CG pole and HP connections skip hanging signs (no arms toward hanging signs)
- Vertical back pole skipped for hanging signs (chains used instead)

### Horizontal Pole Connection for Signs (4/7/26)
- HP bar now extends through both BlockSign and BlockStreetSign blocks
- HP renderer detects adjacent signs and renders horizontal_pole model at sign's block position
- HP shouldConnect includes BlockStreetSign for blockstate arm connections
- HP extractRenderState tracks streetSignDirs separately for bar extension rendering

### Pole Rendering Fixes (4/7/26)
- Regular signs (BlockSign) no longer render the built-in center pole when mounted on a CG pole
  - The sign.json model only contains a pole element; the sign face renders as a separate quad
  - When shifted to a side pole, the body model is skipped to hide the misaligned center pole
  - Standalone signs (not on a pole) still render the built-in pole
- Regular signs do NOT extend center pole up/down — they mount on CG poles to the side
- Traffic lights extend vertical pole toward signs/street signs above and below
  - When shifted to a side pole, the back pole only renders if bridging blocks both above AND below
  - Prevents offset pole stub when TL has a sign on only one side
- BlockTrafficLight.updateHorizontalBar() now recognizes BlockStreetSign as a connectable neighbor
- TL renderer recognizes BlockStreetSign for horizontal bar rendering toward adjacent signs
- CG pole shouldConnect skips hanging street signs

### NeoForge Upgrade
- Upgraded NeoForge from 21.11.38-beta to 21.11.42 stable

### Bug Fixes
- Fixed SIGN_REPO.init() never called — lazy initialization in renderer on first render
- Fixed X11 auth error on runClient — xhost +local: in build.gradle doFirst block
- Still needs improvements: text line rendering on sign face

### Crossing Gate Pole
- BlockCrossingGatePole now uses ROTATION_16 for 16-direction rotation
- Implements EntityBlock with RotatableBlockEntity (registered in ModBlockEntities)
- RenderShape.INVISIBLE — block entity renderer handles body + connection arms
- Connection arms rendered via BE renderer using traffic_light_pole_arm model based on NORTH/SOUTH/EAST/WEST properties
- Keeps existing auto-connect behavior for adjacent blocks (HP, TL, CG poles, signs)

### Street Sign System (full 1.12.2 port) (4/9-4/10/26)
- Multi-sign stacking: up to 4 plates per block, right-click with street sign item to stack
- Sneak + place to put sign in adjacent block instead of stacking
- Per-plate rotation: each plate has independent rotation (0-15), GUI direction button cycles it
- Per-plate color: each plate has own color (green/red/blue/yellow) from GUI color buttons
- 2-line text per plate with auto-scaling to fit 4px plate height
- 1.12.2 texture: original street_sign.png with 4-color UV rows for face colors
- 3D edge faces: all 6 faces per plate (front/back/top/bottom/left/right) rendered dynamically
- Edge faces sample white border pixel from sign texture for clean edges
- GUI overhaul: stacked plate preview, clickable + Tab to switch plates, colored button text
- Direction button (N/S, E/W, NE/SW, etc.) placed between color buttons and Done button
- Retroreflective text: always fullbright (0xF000F0) regardless of ambient light
- Pole mounting: CG pole detection with 7/16 shift so plate edge stops at pole column face
- Street signs excluded from ALL arm rendering (arm models are full-block height, poke through 4px plate)
- CG pole arms skip street sign neighbors (same reason)
- HP doesn't render arms toward street signs
- Block model skipped for street signs — all plates rendered dynamically as custom geometry
- Standalone signs float without center pole (like vanilla signs)
- Hanging signs: plates at Y 15/16 offset, bracket chains connect at Y 19
- Dynamic hitbox based on plate count and pole detection
- Backwards compatibility: old single/dual text format auto-converts to 1+ plates
- Dye interaction: change text color with dye items
- Glow ink sac: toggle glowing text
- Screwdriver bypasses GUI for rotation

### Horizontal Traffic Light Frame Pole Fix (4/10/26)
- Horizontal TL frames no longer shift toward the pole when mounted on horizontal poles
- Horizontal pole extends full-length bar into horizontal TL frame blocks for proper connection
- Horizontal TL frames excluded from bar-through-frame rendering (no middle bar shift)
- Horizontal TL frames excluded from shiftToPole logic entirely
- Fixes: frames no longer overlap when placed side-by-side on horizontal poles
- Only affects horizontal TL frames — vertical TLs still shift and connect as before

### Street Sign GUI Scaling Fix (4/14/26)
- GUI dynamically scales plate sizes for high GUI scale settings (scale 5+)
- Cached layout values computed in init(), shared with render()/mouseClicked()
- Direction button now updates per-plate when switching via click or Tab

### Street Sign HP Mounting (4/14-16/26)
- Street signs mount on horizontal poles: plates center on HP bar (Y 6-10)
- Front-only rendering when HP-mounted (back face, edges, and back text hidden)
- Sign shifts 13/16 toward HP for flush mounting
- Placement rotation +180 so sign faces toward player
- HP no longer renders arm/bar toward signs or street signs (signs shift to pole instead)
- Hitbox matches rendered position (Y offset + 13px shift toward HP)
- CG pole arms still skip street signs (arm models poke through 4px plate)

### Credits & Authors
- Added Gamearoo to authors list and credits for contributing items and textures
- Added AM9327 to credits for helping port the mod to the latest versions of Minecraft
- Updated authors: CSX8600 (https://github.com/CSX8600), KillerMapper, Gamearoo (https://github.com/GamearooCode), AM9327 (https://github.com/AM9327)